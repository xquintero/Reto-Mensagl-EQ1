ssh jumping --> .ssh/config
proxyjump
ssh bd (ejemplo)

1. IP Publica
2. Tabla de rutas

1. Gateway NAT en IP Publica
2. Tabla rutas a traves de ese gatewat nat

PostgreSQL --> Cambiar directiva para permitir conexiones